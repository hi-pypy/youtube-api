/**
 * Dummy mediaManager file.
 * Simulates processing of media files before upload.
 *
 * @param {string} videoFileName - The name of the video file to process.
 */
function processMedia(videoFileName) {
  console.log(`Processing media for video: ${videoFileName}`);
}

module.exports = {
  processMedia
};